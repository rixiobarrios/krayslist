<h1>New Listing</h1>

<%#= render 'form', listing: @listing %>

<%#= form_with(model: listing) do |form| %>
<%#= form_with(model: listing, enctype: "multipart/form-data") do |form| %>
<%= form_with(model: @listing, enctype: "multipart/form-data") do |form| %>
  <%# if listing.errors.any? %>
  <% if @listing.errors.any? %>
    <div id="error_explanation">
      <!--<h2><%#= pluralize(listing.errors.count, "error") %> prohibited this listing from being saved:</h2>-->
      <h2><%= pluralize(@listing.errors.count, "error") %> prohibited this listing from being saved:</h2>

      <ul>
        <%# listing.errors.each do |error| %>
        <% @listing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!--<div class="field">-->
  <div class="form-group">
    <%#= form.label :seller_id %>
    <%#= form.number_field :seller_id %>
    <%#= form.collection_select :seller_id, User.order(:username), :id, :username, include_blank: true %>
    <%#= form.hidden_field :seller_id, value: current_user.id %>
    <%= form.hidden_field :seller_id, value: current_user.id, class: "form-control" %>
  </div>

  <!--<div class="field">
    <%#= form.label :buyer_id %>
    <%#= form.number_field :buyer_id %>
  </div>-->

  <!--<div class="field">-->
  <div class="form-group">
    <%= form.label :title %>
    <%#= form.text_field :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <!--<div class="field">-->
  <div class="form-group">
    <%= form.label :image %><br/>
    <%#= form.text_field :image %>
    <%= form.file_field :image, class: "custome-file-input" %>
  </div>

  <!--<div class="field">-->
  <div class="form-group">
    <%= form.label :details %>
    <%= form.text_area :details, class: "form-control form-control-lg" %>
  </div>

  <!--<div class="field">
    <%#= form.label :location_id %>
    <%#= form.number_field :category_id %>
    <%#= form.collection_select :location_id, Location.order(:city), :id, :city, include_blank: true %>
    <%#= form.hidden_field :location_id, value: cookies[:location] %>
  </div>-->

  <!--<div class="field">-->
  <div class="form-group">
    <%#= form.label :category_id %>
    <%= form.label :city_and_category %>
    <%#= form.number_field :category_id %>
    <%#= form.collection_select :category_id, Category.order(:title), :id, :title, include_blank: true %>
    <%= form.collection_select :category_id, Category.order(:title), :id, :city_and_title, include_blank: true, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-outline-primary btn-block" %>
  </div>
<% end %>

<%#= link_to 'Back', listings_path %>
<%= link_to 'Back', :back %>
