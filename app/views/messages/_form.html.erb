<%= form_with(model: message) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
        <% message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!--<div class="field">-->
  <div class="form-group">
    <%#= form.label :listing_id %>
    <%#= form.number_field :listing_id %>
    <%#= form.collection_select :listing_id, Listing.order(:title), :id, :title, include_blank: true %>
    <%#= form.text_field :listing_id, value: params.fetch("query_listing_id") %>
    <%#= form.text_field :listing_id, value: params.fetch("query_listing_id") %>
    <%= form.hidden_field :listing_id, value: params.fetch("query_listing_id"), class: "form-control" %>
  </div>

  <!--<div class="field">-->
  <div class="form-group">
    <%#= form.label :sender_id %>
    <%#= form.number_field :sender_id %>
    <%#= form.collection_select :sender_id, User.order(:username), :id, :username, include_blank: true %>
    <%# form.text_field :sender_id, value: current_user.id %>
    <%= form.hidden_field :sender_id, value: current_user.id, class: "form-control" %>
  </div>

  <!--<div class="field">-->
  <div class="form-group">
    <%#= form.label :recipient_id %>
    <%#= form.number_field :recipient_id %>
    <%#= form.collection_select :recipient_id, User.order(:username), :id, :username, include_blank: true %>
    <%#= form.text_field :recipient_id, value: params.fetch("query_recipient_id") %>
    <%#= form.text_field :recipient_id, value: params.fetch("query_recipient_id") %>
    <%#= form.hidden_field :recipient_id, value: params.fetch("query_recipient_id") %>
    <%= form.hidden_field :recipient_id, value: params.fetch("query_recipient_id"), class: "form-control" %>
  </div>

  <!--<div class="field">-->
  <div class="form-group">
    <%= form.label :body %>
    <%#= form.text_field :body %>
    <%= form.text_field :body, class: "form-control form-control-lg" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-outline-primary btn-block" %>
 </div> 

<% end %>
