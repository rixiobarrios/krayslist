<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <div class="container">
    <%= link_to image_tag('logo_small.png', :size => "200x40"), locations_path %> 

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <%# if user_signed_in? %>
        <% if user_signed_in? && current_user.username == "admin" %>
          <li class="nav-item">
            <%= link_to locations_path(current_user.username), class: "nav-link" do %>
              <i class="fas fa-compass fa-fw"></i>

              Locations 
            <% end %>
          </li>
            <%= link_to categories_path(current_user.username), class: "nav-link" do %>
              <i class="fa fa-th-large"></i>

              Categories 
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to users_path(current_user.username), class: "nav-link" do %>
              <i class="fas fa-users fa-fw"></i></i>

              Users
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to messages_path(current_user.username), class: "nav-link" do %>
              <i class="fa fa-envelope"></i></i>

              Messages
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to listings_path(current_user.username), class: "nav-link" do %>
              <i class="fa fa-list"></i></i>

              listings
            <% end %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to new_listing_path, class: "nav-link" do %>
              <i class="fas fa-plus-circle"></i>

              Add listing
            <% end %>
          </li>

          <li class="nav-item">
            <%= link_to user_path(current_user.username), class: "nav-link" do %>
              <i class="fas fa-home fa-fw"></i>

              Your profile
            <% end %>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="fa fa-user"></i>
              <%= current_user.username.capitalize %>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to "Edit profile", edit_user_registration_path, class: "dropdown-item" %>
              <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
          <div>
            <%= image_tag current_user.avatar.url, class: "rounded-circle", width: "35px", alt: "avatar" %>
          </div>

        <% else %>

          <li class="nav-item">
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
          </li>

          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>

      </ul>
    </div>
  </div>
</nav>
